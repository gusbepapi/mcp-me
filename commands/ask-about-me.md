---
description: Ask a question about the user's personal profile via the mcp-me MCP server.
---

Ask the following question about the user using the `ask_about_me` MCP tool:

$ARGUMENTS

If the `me` MCP server is not connected, tell the user to install mcp-me (`npx -y mcp-me serve`) and run `mcp-me init` first.

Present the answer clearly, citing specific facts from the profile.
